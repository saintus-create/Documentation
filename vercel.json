{
    "buildCommand": "cd docs && npm install && npm run build",
    "outputDirectory": "docs/.svelte-kit",
    "installCommand": "npm install --prefix docs",
    "framework": null,
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./docs"
}