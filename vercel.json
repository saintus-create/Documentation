{
    "buildCommand": "npm --prefix docs run build",
    "outputDirectory": "docs/build",
    "installCommand": "npm --prefix docs install",
    "framework": null,
    "ignoreCommand": "git diff --quiet HEAD^ HEAD ./docs"
}